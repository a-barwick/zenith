# Zenith website

The engineering leadership site for Zenith.

It presents the project’s compile-time safety case, deployment model, planned
language guarantees, compatibility posture, and milestone path using the
source-of-truth documents in the parent repository.

## Commands

```bash
npm install
npm run dev
npm run build
npm test
```

The site uses vinext and is packaged for deployment with OpenAI Sites.
